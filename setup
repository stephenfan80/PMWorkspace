#!/usr/bin/env bash
set -euo pipefail

HOST="codex"
if [ "${1:-}" = "--host" ]; then
  HOST="${2:-codex}"
fi

ROOT="$(cd "$(dirname "$0")" && pwd)"
exec "$ROOT/bin/product-agent-upgrade" --host "$HOST"
