{
  "id": "pmworkspace-run-owner-child-reuse",
  "title": "主入口创建 run，子 skill 复用当前 run",
  "category": "pm-workspace-runtime",
  "primary_skill": "pm-workspace",
  "prompt": "用户从 $pm-workspace 进入深度交付，然后被路由到 $pm-jobs 或 $pm-autoplan。",
  "expected": {
    "mode": "deep",
    "status": "待确认",
    "must_stop": false,
    "gate": "run 复用",
    "required_behaviors": [
      "主入口创建 run",
      "子 skill 复用当前 run",
      "子 skill 直调且无当前 run 时才创建 run"
    ],
    "forbidden_behaviors": [
      "子 skill 覆盖主入口 run",
      "每个子 skill 都无条件创建新 run",
      "路由后丢失 run_id"
    ]
  },
  "contract_checks": [
    {
      "path": "pmworkspace-shared/references/runtime-kernel.md",
      "contains": "`$pm-workspace` 是路由型会话的 run owner"
    },
    {
      "path": "pmworkspace-shared/references/runtime-kernel.md",
      "contains": "子 skill 发现已有当前 run 时复用当前 run"
    },
    {
      "path": "pm-autoplan/SKILL.md",
      "contains": "复用当前 run，不要重新 `pmw-run start`"
    },
    {
      "path": "pm-jobs/SKILL.md",
      "contains": "用户直接调用 `$pm-jobs` 且没有当前 run"
    }
  ]
}
