#!/usr/bin/env bash
set -euo pipefail

cat <<'EOF'
PMWorkspace 已安装。

重启 Codex 后，从这里开始：

  使用 $pm-workspace 显示欢迎引导，并帮我选择合适的产品工作流。

常用第一句话：

  使用 $pm-workspace 帮我梳理一个新产品想法：<一句话想法>
  使用 $pm-workspace 读取这份 PRD / Zoon 文档，并生成可确认的产品简报：<链接或内容>
  使用 $pm-workspace 帮我对这个功能做 3 个移动端原型方向，每个方案单独出图：<功能描述>
  使用 $pm-workspace 帮我做线上流程里的新页面：<页面说明>，我会补充上一步或相似结果页截图

PMWorkspace 工作流：
  产品追问 -> 选择题拍板 -> 线上参考检查 -> 产品简报 + Zoon 在线文档 -> 原型方案 -> 交付稿
EOF
