{
  "id": "brief-confirmation-current-revision-required",
  "title": "产品简报锁定必须匹配当前 input_revision",
  "category": "pm-workspace-runtime",
  "primary_skill": "pm-brief",
  "prompt": "如果产品简报确认早于最新 input_revision，不能把本轮 brief 锁定并进入原型。",
  "expected": {
    "mode": "maintenance",
    "status": "待确认",
    "must_stop": true,
    "gate": "产品简报确认",
    "required_behaviors": [
      "brief_lock 必须匹配当前 task_digest/input_revision",
      "旧 revision 的确认被忽略",
      "readiness first_blocker 指向产品简报"
    ],
    "forbidden_behaviors": [
      "把旧确认用于新材料",
      "只凭 Markdown 的确认状态放行",
      "跳过 brief_lock 直接出图"
    ]
  },
  "contract_checks": [
    {
      "path": "bin/pmw-artifact",
      "contains": "event_matches_current_task"
    },
    {
      "path": "pm-brief/SKILL.md",
      "contains": "当前 `input_revision`"
    }
  ],
  "command_checks": [
    {
      "command": [
        "bash",
        "-lc",
        "tmp=$(mktemp -d); export PMW_HOME=\"$tmp\" PMW_PROJECT_SLUG=stale-confirm; python3 - <<'PY'\nimport json, os, pathlib\nroot=pathlib.Path(os.environ['PMW_HOME'])/'projects'/os.environ['PMW_PROJECT_SLUG']; (root/'runs').mkdir(parents=True)\nbrief=root/'brief.md'; brief.write_text('确认状态：已对齐\\n不可虚构：不能展示真实收益承诺\\n', encoding='utf-8')\nproj={'slug':os.environ['PMW_PROJECT_SLUG'],'current_run_id':'run1','latest_run_id':'run1','latest_run_status':'已开始','current_task_digest':'digest-new','current_input_revision':'rev-new','latest_brief':str(brief),'latest_brief_version':'v1'}\n(root/'project.json').write_text(json.dumps(proj, ensure_ascii=False), encoding='utf-8')\n(root/'artifact-flow.jsonl').write_text(json.dumps({'kind':'product_brief','run_id':'run1','status':'已对齐','path':str(brief),'task_digest':'digest-new','input_revision':'rev-new'}, ensure_ascii=False)+'\\n', encoding='utf-8')\n(root/'runs'/'run1.jsonl').write_text(json.dumps({'event':'decision','run_id':'run1','status':'已对齐','title':'产品简报确认','summary':'旧确认','task_digest':'digest-new','input_revision':'rev-old'}, ensure_ascii=False)+'\\n', encoding='utf-8')\nPY\nbin/pmw-dashboard readiness --target prototype --json"
      ],
      "exit_code": 0,
      "contains": [
        "\"first_blocker\": \"产品简报\"",
        "\"ready\": false",
        "当前 run 未记录 brief_lock"
      ]
    }
  ]
}
