{
  "id": "p0-operation-router-no-generic-fallback",
  "title": "P0：operation router 不允许 generic fallback",
  "category": "pm-workspace-runtime",
  "primary_skill": "pm-workspace",
  "prompt": "PMW 命中后必须由 operation router 或 controller 接管，不能退回普通产品分析或普通出图。",
  "expected": {
    "mode": "maintenance",
    "status": "可检查",
    "must_stop": true,
    "gate": "no generic fallback",
    "required_behaviors": [
      "trigger guard 输出 forbidden_fallbacks",
      "routing.md 要求 guard -> operation router -> router 指定命令",
      "blocked_unknown 要求用户确认 operation"
    ],
    "forbidden_behaviors": [
      "PMW runtime 未接管时继续普通方案输出",
      "router 低置信度时静默 intake"
    ]
  },
  "contract_checks": [
    {
      "path": "pmworkspace-shared/references/routing.md",
      "contains": "pmw-operation-router classify"
    },
    {
      "path": "bin/pmw-operation-router",
      "contains": "blocked_unknown"
    }
  ],
  "command_checks": [
    {
      "command": [
        "bash",
        "-lc",
        "bin/pmw-trigger-guard --text 'PMW 在这个图基础上修改，不要改100元券样式' --skill pm-workspace --json"
      ],
      "exit_code": 0,
      "contains": [
        "\"required_next\": \"pmw-operation-router classify\"",
        "generic_product_plan",
        "imagegen_without_permit"
      ]
    }
  ]
}
