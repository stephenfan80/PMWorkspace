# visual_baseline：标注截图

状态：已登记
来源：用户上传标注截图
参考路径：`examples/assets/trial-form-annotated-redacted.png`（占位示例，不包含真实截图）
参考资产策略：公开样例只记录标注语义和编辑边界；真实项目必须读取用户上传截图，并以截图物理像素作为基线。

## 截图信息

| 字段 | 示例值 |
|---|---|
| 参考像素尺寸 | `1179 x 2556` |
| 目标输出像素 | `1179 x >=2556` |
| 画布模式 | `physical_longboard` |
| 生成模式 | `screenshot_edit` |

## 标注解释

- 红框：删除表单页里的解释模块。
- 绿框：保留手机号输入和主要按钮。
- 用户确认：产品意图不改变。

## 编辑范围

- 删除红框解释模块。
- 收紧表单页纵向密度。
- 保留手机号输入、主按钮、顶部导航和底部安全区。
- 必要说明后置到结果页。

## 保留区域

- 页面标题和返回导航。
- 手机号输入框。
- 主按钮。
- 隐私 / 协议入口。
- 已接受的视觉风格。

## 不可改动

- 不新增字段。
- 不改变主按钮含义。
- 不把结果承诺前置到表单页。
- 不重画整页风格。
